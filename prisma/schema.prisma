generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../db.sqlite3"
}

model User {
  id        Int     @default(autoincrement()) @id
  email     String  @unique
  password  String
}
